(defun primes (num)

(setq count 0.0)
(while (< count num )
(setq div_double (/ num count))
(setq modulus (rem num count))
(if (= modulous 0)
(member factors div_double)
)

(setq factors (append factors (list div_double)))

)

)